version: 1
applications:
  - appRoot: .
    frontend:
      phases:
        preBuild:
          commands:
            # Install root deps for ampx CLI (ci = deterministic from lockfile)
            - npm ci
            # Deploy backend only when backend files changed; skip for frontend-only commits.
            - |
              if git diff --name-only HEAD~1 HEAD 2>/dev/null | grep -qE '^amplify/|^shared/|^package\.json'; then
                echo "Backend files changed — deploying backend..."
                npx ampx pipeline-deploy --branch $AWS_BRANCH --app-id $AWS_APP_ID
                npx ampx generate outputs --branch $AWS_BRANCH --app-id $AWS_APP_ID
              else
                echo "Frontend-only change — using committed amplify_outputs.json"
              fi
            - cp amplify_outputs.json frontend/amplify_outputs.json
            - npm ci --prefix frontend
        build:
          commands:
            - npm run build --prefix frontend
      artifacts:
        baseDirectory: frontend/dist
        files:
          - '**/*'
      cache:
        paths:
          - ~/.npm/**/*
    # CORS: production domain only. Preview branches use Amplify's built-in CORS handling.
    customHeaders:
      - pattern: '**/*'
        headers:
          - key: 'Access-Control-Allow-Origin'
            value: 'https://monarchy.gurum.se'
          - key: 'Access-Control-Allow-Methods'
            value: 'GET, POST, PUT, DELETE, OPTIONS'
          - key: 'Access-Control-Allow-Headers'
            value: 'Content-Type, Authorization'
